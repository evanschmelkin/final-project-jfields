<div id="<%= dom_id todo %>">
  <p>
    <strong>Title:</strong>
    <%= todo.title %>
  </p>

  <p>
    <strong>Priority:</strong>
    <%= todo.priority %>
  </p>

  <p>
    <strong>Completed:</strong>
    <%= todo.completed %>
  </p>

  <!-- commented out by evan because we shouldn't show user id/username
  <p>
    <strong>User:</strong>
    <%= todo.user_id %>
  </p>
-->
  <p>
    <strong>Category:</strong>
    <%= todo.category.name %> <!-- Shows the name instead of the ID -->
  </p>

</div>
