<p style="color: green"><%= notice %></p>

<% content_for :title, "Todos" %>

<h1>Completed Todos</h1>

<div id="todos">
  <% @todos.each do |todo| %>
    <% if todo.completed == true %>


<%= render todo %>
    <p>
      <%= link_to "Show this todo", todo, class: "btn btn-success" %>
    </p>
  <% end %>
  <%end %>
</div>

<%= link_to "Create ToDo", new_todo_path, class: "btn btn-success" %>

